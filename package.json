{
  "name": "lostlister",
  "version": "1.0.0",
  "description": "LostLister is a open source web based solution to help those students who lost something inside our university.\r Currently working indipendently, and not affiliated by the university.",
  "main": "index.js",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix client",
    "start": "cd client && npm start",
    "server": "nodemon server/src/index.js",
    "client": "cd client && npm start",
    "build": "npm run build --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.4"
  }
}
